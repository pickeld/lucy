# =============================================================================
# Lucy — Environment Variables (.env)
# =============================================================================
#
# FIRST-RUN SEED ONLY
#
# This file is used ONLY on the very first startup to seed the SQLite
# settings database (data/settings.db). After that, all configuration
# is managed through the Settings UI page and stored in SQLite.
#
# You can delete this file after the first run — it will not be read again.
# To change settings after initial setup, use the Settings page in the UI
# or the PUT /config API endpoint.
#
# If the SQLite database is deleted, the app will re-seed from this file.
# =============================================================================

# --- API Keys (Secrets) ---
OPENAI_API_KEY=your-openai-api-key
GOOGLE_API_KEY=your-google-api-key
WAHA_API_KEY=your-waha-api-key

# --- LLM Configuration ---
LLM_PROVIDER=openai
OPENAI_MODEL=gpt-4o
OPENAI_TEMPERATURE=0.7
GEMINI_MODEL=gemini-pro
GEMINI_TEMPERATURE=0.7
IMAGE_PROVIDER=openai
IMAGEN_MODEL=imagen-3.0-generate-002

# --- RAG Configuration ---
RAG_COLLECTION_NAME=knowledge_base
RAG_MIN_SCORE=0.2
RAG_MAX_CONTEXT_TOKENS=3000
RAG_DEFAULT_K=10
EMBEDDING_MODEL=text-embedding-3-large

# --- WhatsApp / WAHA ---
WAHA_BASE_URL=http://waha:3000
WAHA_SESSION_NAME=default
WEBHOOK_URL=http://app:8765/webhook
CHAT_PREFIX=??
DALLE_PREFIX=!!
DALLE_MODEL=dall-e-3

# --- WAHA Media Storage ---
# Required for voice transcription, image description, etc.
WAHA_MEDIA_STORAGE=LOCAL
WHATSAPP_FILES_FOLDER=/app/.media
WHATSAPP_FILES_LIFETIME=0
WHATSAPP_DOWNLOAD_MEDIA=true
# MIME types WAHA will download and store. Must include audio/* for voice transcription.
WHATSAPP_FILES_MIMETYPES=image/jpeg,image/png,image/gif,image/webp,audio/ogg,audio/mpeg,audio/mp4,audio/wav,audio/webm,video/mp4,video/3gpp,application/pdf

# --- Infrastructure ---
# Use 'redis'/'qdrant' as host when running in Docker, 'localhost' when local
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_TTL=604800
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# --- App Settings ---
LOG_LEVEL=DEBUG
SESSION_TTL_MINUTES=30
SESSION_MAX_HISTORY=20
